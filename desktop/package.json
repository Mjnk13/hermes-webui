{
  "name": "hermes-webui-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Electron desktop shell for Hermes WebUI Browser Workbench native WebContentsView integration.",
  "main": "out/main/index.js",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "preview": "electron-vite preview",
    "check": "node --check src/main/index.cjs && node --check src/main/browser-session-policy.cjs && node --check src/main/browser-window-policy.cjs && node --check src/preload/index.cjs",
    "test": "node --test test/*.test.cjs"
  },
  "devDependencies": {
    "electron": "^33.4.11",
    "electron-vite": "^2.3.0",
    "vite": "^5.4.21"
  }
}
